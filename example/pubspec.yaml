name: example
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.1.5 <4.0.0'

dependencies:
  pods:
    path: ../pods
  pocketbase_pods:
    path: ../pocketbase_pods
  flutter_pods_ui:
    path: ../flutter_pods_ui
  flutter_pods:
    path: ../flutter_pods
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  flutter_riverpod: ^2.3.10
  flutter_hooks: ^0.20.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

